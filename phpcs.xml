<?xml version="1.0"?>
<ruleset name="PHP_CodeSniffer">
    <description>PHP_CodeSniffer configuration</description>
    <rule ref="PSR12">
        <exclude name="Squiz.Classes.ValidClassName" />
        <exclude name="Generic.Files.LineLength" />
        <exclude name="Squiz.Commenting.FunctionComment.IncorrectParamVarName" />
        <exclude name="Squiz.Commenting.FunctionComment.InvalidReturn" />
    </rule>

    <arg name="bootstrap" value="vendor/zebra-north/phpcs-short-types/short-types.php"/>

    <rule ref="Modernize" />
    <rule ref="NormalizedArrays" />

    <!-- exclude our migrations directory from the violation check-->
    <exclude-pattern>*.blade.php</exclude-pattern>
    <exclude-pattern>vendor</exclude-pattern>

    <!-- ignore warnings and display ERRORS only -->
    <arg  value="np"/>

    <rule ref="Universal.Namespaces.DisallowCurlyBraceSyntax">
        <exclude name="Universal.Namespaces.DisallowCurlyBraceSyntax.Forbidden" />
    </rule>

    <rule ref="Squiz.Commenting.FunctionComment">
        <exclude name="Squiz.Commenting.FunctionComment.MissingParamTag" />
        <exclude name="Squiz.Commenting.FunctionComment.MissingParamComment" />
        <exclude name="Squiz.Commenting.FunctionComment.ParamCommentFullStop" />
        <exclude name="Squiz.Commenting.FunctionComment.EmptyThrows" />
        <exclude name="Squiz.Commenting.VariableComment.MissingReturn" />
        <exclude name="Squiz.Commenting.FunctionComment.ParamCommentNotCapital" />

        <!-- The signature carries the types, so a docblock only has to add what
             the signature cannot say: array shapes, and why. MissingParamTag is
             excluded above for that reason; ParamNameNoMatch matches a partial
             @param list positionally, which makes that exclusion unusable, and
             MissingReturn demands a tag that only repeats the return type. -->
        <exclude name="Squiz.Commenting.FunctionComment.ParamNameNoMatch" />
        <exclude name="Squiz.Commenting.FunctionComment.MissingReturn" />

        <!-- A test method's name is its documentation, and the arrange/act/assert
             body is the rest of it. Everything else in this ruleset still applies
             to tests. -->
        <exclude-pattern>tests/</exclude-pattern>
    </rule>

    <rule ref="Squiz.Commenting.FunctionCommentThrowTag">
        <exclude name="Squiz.Commenting.FunctionCommentThrowTag.Missing" />
    </rule>

    <rule ref="Squiz.Commenting.ClassComment">
        <exclude-pattern>tests/</exclude-pattern>
    </rule>

    <rule ref="Squiz.Commenting.VariableComment" >
        <!-- Put back in at some point -->
        <exclude name="Squiz.Commenting.VariableComment.Missing" />
    </rule>

    <!-- Keeping this in here for future use
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="180"/>
            <property name="absoluteLineLimit" value="210"/>
        </properties>
    </rule>
    -->
</ruleset>
